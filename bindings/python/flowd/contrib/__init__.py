"""Optional, batteries-included integrations (extra dependencies).

``flowd.contrib.anthropic`` needs the ``anthropic`` SDK; install it with the
``[anthropic]`` extra (``pip install "flowd[anthropic]"``).
"""
